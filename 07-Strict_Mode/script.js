//'use strict';  - uncomment for enable strict mode
// When strict mode is enabled, some silent errors are converted to throw errors, which makes debugging easier

console.log('Strict Mode');

let user = 'surya';

use = 'siva';

console.log(user);

const interface = 'CAr';  
console.log(interface);
//interface , private are reserved keywords, we cant use that for variable name
const private = 'Jack';
console.log(private); 